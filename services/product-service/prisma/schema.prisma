generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Book {
  isbn        String      @id
  title       String  
  author      String 
  genre       String
  price       Decimal  
  stock       Int     
}